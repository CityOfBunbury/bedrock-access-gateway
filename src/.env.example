# AWS Credentials (replace with your actual credentials or use IAM roles/profiles)
AWS_ACCESS_KEY_ID=YOUR_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=YOU_SECRET_ACCESS_KEY
# AWS_SESSION_TOKEN=YOUR_AWS_SESSION_TOKEN # Uncomment and set if using temporary credentials

# AWS Region
AWS_REGION=ap-southeast-2

# Optional: Specify a default agent name (must match a name in agents.yaml)
# DEFAULT_AGENT=MY-AGENT-DEV

# Optional: API Route Prefix
# API_ROUTE_PREFIX=/api/v1

# Optional: Debug Mode (set to "true" to enable)
# DEBUG=false 